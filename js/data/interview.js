/* Interview questions for the Python and Agentic AI tracks (RAG lessons define their own). */
const INTERVIEW = {
p01:{q:"What happens when you run a Python script?", a:"The interpreter reads the file from top to bottom, compiles each part to bytecode and executes it line by line. Output from print goes to the screen, and an error stops the program with a traceback unless it's handled."},
p02:{q:"What's the difference between an int, a float and a string, and why does it matter?", a:"An int is a whole number, a float is a decimal and a string is text. Operations depend on type: \"5\" + \"5\" joins text into \"55\" while 5 + 5 is 10. Data from files and APIs often arrives as strings, so converting types correctly prevents bugs."},
p03:{q:"How would you build a prompt template safely in Python?", a:"Use an f-string or a template with named placeholders, clean inputs first (strip, normalize), and keep user-provided text clearly delimited, for example in XML tags, so it can't be confused with instructions."},
p04:{q:"What values are falsy in Python?", a:"False, None, 0 and 0.0, empty strings, and empty collections like [], {}, set() and (). Everything else is truthy, so \"if items:\" means \"if the list isn't empty\"."},
p05:{q:"When would you use a while loop instead of a for loop?", a:"When you don't know in advance how many iterations you need, such as retrying until success or polling until a job finishes. Always make sure the condition eventually becomes false or include a maximum, to avoid infinite loops."},
p06:{q:"What's the difference between a list and a tuple?", a:"Lists are mutable: you can add, remove and change items. Tuples are immutable, which makes them safe for fixed records and usable as dictionary keys. Use lists for collections that change and tuples for fixed groupings like coordinates."},
p07:{q:"Why are dictionary lookups fast?", a:"Dictionaries are hash tables: the key is hashed to find its slot directly, so lookups, inserts and deletes are O(1) on average instead of scanning every item like a list."},
p08:{q:"What's the difference between print and return in a function?", a:"print displays a value but the function still returns None. return hands the value back to the caller so other code can use it. Reusable functions should return values and leave displaying to the caller."},
p09:{q:"How would you make an API call resilient to failures?", a:"Catch specific exceptions, retry only transient errors with exponential backoff and jitter, cap the number of attempts, set timeouts, and log failures. Re-raise or fall back gracefully when retries are exhausted."},
p10:{q:"Why use virtual environments?", a:"Each project gets its own isolated set of packages and versions, so upgrading a library for one project can't break another, and requirements files make the environment reproducible for teammates and servers."},
p11:{q:"What's JSONL and why is it popular for AI datasets?", a:"JSON Lines stores one JSON object per line. You can stream it line by line without loading the whole file, append new records easily, and one corrupt line doesn't ruin the file. That's why evaluation sets, fine-tuning data and logs often use it."},
p12:{q:"What is self in a Python class?", a:"self is the specific instance a method is called on. Python passes it automatically as the first argument, so methods can read and change that object's own attributes."},
p13:{q:"When would you choose composition over inheritance?", a:"When objects have a \"has a\" relationship rather than \"is a\", or when inheritance chains get deep and fragile. Composition, building objects from other objects, keeps classes small and flexible and makes swapping parts easier."},
p14:{q:"What is a generator and when is it useful?", a:"A function that uses yield to produce values one at a time, pausing between them. It's memory-efficient for large or endless data, like reading a huge file line by line or streaming model output."},
p15:{q:"How would you redact PII from text before sending it to an LLM?", a:"Use regular expressions for structured patterns like emails, phone numbers and IDs, plus a named-entity recognition model or a dedicated service for names and addresses. Replace matches with placeholders, test on real samples, and log what was redacted without logging the values."},
p16:{q:"What's the difference between HTTP 401 and 403?", a:"401 means the request isn't authenticated: credentials are missing or invalid. 403 means you're authenticated but not allowed to access that resource. Neither should be retried automatically."},
p17:{q:"Why is vectorized NumPy code faster than a Python loop?", a:"The operation runs in optimized compiled code over a whole array at once, avoiding Python's per-item interpreter overhead. It also uses efficient memory layouts and CPU instructions that process many numbers together."},
p18:{q:"How does binary search work and what's its complexity?", a:"On sorted data, compare the target with the middle item, then discard the half that can't contain it, and repeat. Each step halves the search space, so it takes O(log n) steps: about 20 for a million items."},
p19:{q:"What does cosine similarity measure?", a:"How aligned two vectors are, regardless of their length: the dot product divided by the product of their magnitudes. It ranges from -1 to 1, and for text embeddings, higher means more similar meaning."},
p20:{q:"Describe how a tool-using AI agent works.", a:"The model receives the task and tool descriptions, then either answers or requests a tool call. The application runs the tool, returns the result, and calls the model again, looping until the model gives a final answer or a limit is reached. Guardrails like step limits, validation and approvals sit in the application code."},
a01:{q:"Why do LLMs hallucinate, and how do you reduce it?", a:"They generate statistically likely text rather than looking up facts, so gaps get filled with plausible-sounding content. Reduce it by grounding answers in provided documents or tool results, allowing \"I don't know\", asking for citations or quotes, and measuring with evals."},
a02:{q:"The Messages API is stateless. What does that mean for your application?", a:"The API doesn't remember previous calls, so the application must store the conversation and send the relevant history with every request. That also means you control what's in context, which lets you trim, summarize or cache it."},
a03:{q:"How would you cut the cost of an LLM application without hurting quality?", a:"Measure first, then route simple tasks to smaller models (verified with evals), use prompt caching for repeated prefixes, batch non-urgent work, trim unnecessary context, cap output length and cache repeated answers."},
a04:{q:"What makes a prompt robust?", a:"Clear, specific instructions with context about the goal and audience, structure with XML tags separating instructions from data, a few diverse examples of the desired output, explicit handling of edge cases, and testing against a fixed set of real inputs."},
a05:{q:"How do you get reliable structured output from an LLM?", a:"Define the exact schema, use the API's structured output features or a forced tool call with that schema, then validate the result in code with something like Pydantic. On failure, retry with the validation error included or fall back safely."},
a06:{q:"Walk through a tool-use round trip with Claude.", a:"Send messages plus tool definitions. The response has stop_reason tool_use and tool_use blocks with ids and inputs. Run each tool, append the assistant message, then a user message with tool_result blocks carrying matching tool_use_ids, and call the API again for the final answer."},
a07:{q:"What makes a good tool for an AI agent?", a:"A clear, specific name and a detailed description of when to use it, few well-typed inputs with enums and formats, concise and relevant outputs, actionable error messages, least-privilege access and idempotent writes where possible."},
a08:{q:"How do you prevent an agent from running forever?", a:"Set a maximum number of turns, a cost or token budget and a time limit, handle every stop_reason explicitly, detect repeated identical tool calls, and let humans stop runs. Log each turn so stuck runs can be diagnosed."},
a09:{q:"When would you use a workflow instead of an agent?", a:"When the steps are known in advance. Workflows are more predictable, cheaper, faster and easier to test. Use agents when the path can't be predicted and the model needs to choose tools and steps dynamically."},
a10:{q:"How do you manage context in a long-running agent?", a:"Keep recent turns, summarize older ones, clear large tool results once used, store durable facts in external memory and retrieve them when relevant, and delegate deep work to subagents that return summaries."},
a11:{q:"What's the difference between reflection and verification?", a:"Reflection is the model critiquing its own output, which helps but can miss errors. Verification checks results against objective signals like running tests, validating data or confirming a file exists. Prefer verification whenever it's possible."},
a12:{q:"When is a multi-agent architecture worth its cost?", a:"When work splits into independent parts that can run in parallel or needs more context than one window can hold, such as broad research. For tightly coupled, sequential work on shared state, a single agent is usually more reliable and cheaper."},
a13:{q:"What problem does MCP solve?", a:"It standardizes how AI applications connect to tools and data. A server built once works with any MCP-compatible client, instead of writing custom integrations for every app and model combination."},
a14:{q:"How would you set up Claude Code safely for a team repository?", a:"Add a concise CLAUDE.md with conventions and test commands, configure permission rules that allow safe commands, ask for risky ones and deny secrets and destructive commands, add hooks for formatting or checks, and review diffs before merging."},
a15:{q:"How do you decide which agent actions need human approval?", a:"By risk: consider reversibility, blast radius, cost and sensitivity. Reads can be automatic; routine writes can be logged; deletes, external messages, payments and changes to people's records need explicit approval with a clear preview and an audit trail."},
a16:{q:"How do you defend an agent against indirect prompt injection?", a:"Layered defenses: mark external content as untrusted data, restrict tools to read-only while untrusted content is in context, require human approval for sensitive actions, apply least privilege, validate outputs before other systems use them, and red-team the system regularly."},
a17:{q:"How do you build an eval set for an LLM feature?", a:"Start with 20 to 50 real inputs covering common cases, edge cases and past failures, define expected outcomes, prefer code-based graders and use calibrated LLM judges for subjective qualities. Run it on every change and add each production bug as a new case."},
a18:{q:"Which API errors should you retry, and how?", a:"Retry transient errors like 429, 500 and 529 with exponential backoff and jitter, respecting retry-after headers and capping attempts. Don't retry client errors like 400, 401, 403 or 413; fix the request instead."},
a19:{q:"What would you monitor in a production LLM application?", a:"Cost per task, token usage, latency percentiles (p50 and p95), error and retry rates, cache hit rate, stop reasons, tool call success, eval scores over time and user feedback, all traced per request with secrets and personal data redacted."},
a20:{q:"What separates a demo agent from a production agent?", a:"Production agents validate tool inputs, handle errors gracefully, enforce approvals and limits, log and trace every step, have eval suites in CI, manage cost and latency, and protect against prompt injection and data leaks."}
};
for (const l of LESSONS.concat(AGENT_LESSONS)) if (INTERVIEW[l.id]) l.interview = INTERVIEW[l.id];
